import React, { PureComponent } from 'react'
import PropTypes from 'prop-types'

class StudentPad extends PureComponent {
  static propTypes = {
  }

  render() {
    return (

    )
  }
}

export default StudentPad
